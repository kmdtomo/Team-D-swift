# T11-01 Phase 1 corpus audit — 2026-09-01

## Frozen work unit

- Task/lane: T11-01, Lane D/F only.
- Upstream/start ref: `3630266c3cd13b3cd322c998d7cd563cde50cdbd`.
- Frozen synthetic artifact: measurement corpus schema version 2 and the
  deterministic generator/manifest at the start ref.
- Owned files: `scripts/t11_01_measurement_corpus/**`,
  `Fixtures/MeasurementCorpus/**`, and this T11-01 audit/runbook material.
- Tests authored for deferred/in-scope source validation:
  `scripts/t11_01_measurement_corpus/test_lint_corpus.py`, covering an empty
  objectively blocked gate, a complete external 30-capture matrix, external
  file/hash verification, and rights/tamper rejection.
- Intentionally open gates: Swift/package/app build and tests, T11-02 engine
  execution, physical-device acquisition/annotation, Apple/OpenCV decision,
  and downstream measurement accuracy/acceptance.

## Existing asset audit

| Asset | Observed state | Decision |
| --- | --- | --- |
| `corpus-manifest.json` / schema v2 | 18 deterministic synthetic cases; all finite measurement failures have positive/negative fixture evidence; 79/80px, 16/17px, 0.649/0.650, and 23/24px boundaries are present | retain as synthetic input; not physical evidence |
| `generate_synthetic.py` | standard-library, source-created, deterministic RGB PNG output with pinned SHA-256 | retain; no source-Web or personal binary |
| 50/5/40mm marker PDF | tracked deterministic A4 PDF, SHA-256 `4e5158506dbdf66623f281e8b9d7b01f7c23349beeefbf5682030d165b638217` | printable artifact only; printed size still needs a ruler record |
| physical capture CSV | header only at the start ref | 0 physical captures; no completion claim |
| ruler evidence | no record or referenced external evidence at the start ref | no 50.0mm physical confirmation |
| physical images | none tracked or referenced by an approved T11-01 log | do not fabricate or import unreviewed binaries |

The prior physical CSV lint checked only its column names. This slice adds
strict row semantics, explicit marker-print evidence, the exact 30-capture
distribution, rights/PII/annotation review flags, safe relative external paths,
file signatures, hashes, and a fail-closed `--require-physical-gate` mode. It
does not automate or pretend to perform the human rights/PII/ruler review.

## Safe local evidence search

The audit inspected Git history/branches/worktrees and known Team-D Swift
repository/worktree roots for T11-01 physical logs and named ruler/corpus
evidence. Every discovered `physical-corpus-log.csv` had exactly one line (the
header). No additional log row or named ruler-evidence record was found. The
search did not traverse the user's general photo library or copy/open unrelated
personal images.

## Objective blocker

- Status: `blocked`; the task must remain unchecked.
- Facts: physical core is 0/30; reviewed marker-print evidence is 0; external
  image/evidence hashes therefore cannot be verified.
- Owner: physical device operator/user with authority to create and review the
  first-party corpus.
- Release condition: print the frozen marker at 100%, retain a rights/PII-
  reviewed ruler evidence file showing an outer side of 50.0mm, acquire at
  least 30 distinct standard-Camera iPhone captures matching every required
  matrix bucket, complete all annotations and rights basis, then run the strict
  external-root gate successfully. A task owner must review that evidence
  before T11-01 can become `code_ready_unverified` or inform T11-03.

No Apple/OpenCV adoption conclusion is possible from the synthetic corpus or
this source-only audit.
